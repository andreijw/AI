# Core dependencies
gymnasium>=0.29.0
numpy>=1.21.0
pyyaml>=6.0
matplotlib>=3.5.0

# Optional: video recording support (required for --record-video flag)
# pip install moviepy "gymnasium[classic-control]"
# moviepy>=1.0.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
ruff>=0.1.0
